This is a forked version of [lib/pq](https://github.com/lib/pq), modified to
allow access to the underlying TLS connection context so that Certigo can dump
TLS certificates from PostgreSQL database servers.
